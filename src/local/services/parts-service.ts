/**
 * Local Parts Service — barrel re-export from parts/ subdirectory.
 *
 * This file preserves backward compatibility for all existing imports.
 * The actual implementation is split across domain-specific files in ./parts/.
 */

export * from './parts/index';
